package com.private_lbs.taskmaster.total_player.domain.repository;

import com.private_lbs.taskmaster.total_player.domain.TotalPlayer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TotalPlayerRepository extends JpaRepository<TotalPlayer, Long> {
}
